name: arieare
markdown: kramdown
permalink: /weblog/:title
url: "https://arieare.co"
plugins:
  - jekyll-feed
# Jekyll Collections
collections:
  answers:
  pages:
    output: true # Set to render files in _pages folder
    permalink: /:title/ # Set the format of the permalink by default
