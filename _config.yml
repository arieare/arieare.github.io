name: arieare
markdown: kramdown
permalink: /weblog/:year/:month/:day/:title
url: "https://arieare.fyi"
plugins:
  - jekyll-feed
# Jekyll Collections
collections:
  answers:
  pages:
    output: true # Set to render files in _pages folder
    permalink: /:title/ # Set the format of the permalink by default
